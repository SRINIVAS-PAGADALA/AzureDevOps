```yaml
name: My First CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  my-first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t cinema-app ./app
```
