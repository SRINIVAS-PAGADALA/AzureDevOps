apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-cinema-app.fullname" . }}
  labels:
    {{- include "helm-cinema-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-cinema-app.selectorLabels" . | nindent 4 }}
